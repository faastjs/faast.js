{
    "license": "Apache-2.0",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "aws-sdk": "^2.437.0",
        "tar-stream": "^2.0.1"
    },
    "devDependencies": {
        "@types/tar-stream": "^1.6.0",
        "typescript": "^3.4.5"
    }
}
