{
    "license": "Apache-2.0",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@aws-sdk/client-s3": "^3.309.0",
        "tar-stream": "^2.2.0"
    },
    "devDependencies": {
        "@types/tar-stream": "2.2.2",
        "typescript": "5.0.4"
    }
}
