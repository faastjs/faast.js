{
    "license": "Apache-2.0",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "aws-sdk": "^2.616.0",
        "tar-stream": "^2.1.0"
    },
    "devDependencies": {
        "@types/tar-stream": "^1.6.1",
        "typescript": "^3.7.5"
    }
}
