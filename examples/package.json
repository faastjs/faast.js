{
    "license": "Apache-2.0",
    "scripts": {
        "build": "tsc",
        "watch": "tsc -w",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "aws-sdk": "^2.713.0",
        "tar-stream": "^2.1.3"
    },
    "devDependencies": {
        "@types/tar-stream": "^2.1.0",
        "typescript": "^3.9.6"
    }
}
